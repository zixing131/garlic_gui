# 版本提交规则

- 每次提交新版本时，将 GUI 版本号末位递增 1，例如 `0.5.7` → `0.5.8`。
- 版本号统一定义在 `gui/CMakeLists.txt` 的 `GARLIC_GUI_VERSION`，界面、命令行、无头模式、MCP 和应用包元数据共用此值。
- 同步更新 `docs/garlic-gui.md` 顶部的当前版本号；保留历史版本记录不变。
- 提交前构建并检查普通模式与无头模式的 `--version` 输出。
